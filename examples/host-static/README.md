# Static Site Example

A minimal HTML/CSS/JS site to test `m-gpux host static`.

## Deploy

```bash
cd examples/host-static
m-gpux host static --dir .
```

That's it! You'll get a public URL like:
```
https://<profile>--m-gpux-host-<slug>-web.modal.run
```

## Stop

```bash
m-gpux stop
```
